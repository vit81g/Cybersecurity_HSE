1. Какая функция используется для вывода информации на консоль?
print()

2.  Выберите правильно созданную переменную (возможно несколько вариантов):

num = 2

num = float(2)

3. Как получить данные от пользователя?
input()

4. Выберите правильное объявление условной конструкции:

if x > y:

5. Как обозначаются комментарии в Python?

# This is a comment

6. Выберите правильное объявление начала цикла for в Python:

for x in y:

7. Какое ключевое слово используется для остановки цикла?

break

8. Какая из представленных коллекций является упорядоченной, изменяемой и допускает наличие повторяющихся элементов?

Список (list)

9. Какая из представленных конструкций определяет словарь (dict)?

{"name": "apple", "color": "green"}

10. Дан список a = ['foo', 'bar', 'baz', 'qux', 'quux', 'corge'], выберите правильные результаты выполнения команды (возможно несколько вариантов):

print(a[-5:-3]) → ['bar', 'baz']

print(a[4::-2]) → ['quux', 'baz', 'foo']


11. Выберите верное объявление функции в Python:

def my_function():


12. Какое ключевое слово используется для объявления анонимной функции?

lambda

13. Как называется пакетный менеджер в Python?

pip

14. Какое ключевое слово используется в Python для создания исключительной ситуации?

raise

15. Какая библиотека отвечает за время?

time

16. Функция def __init__(?). Какое слово обязательно должно быть внутри круглых скобок (вместо знака вопроса)?

self

17. Написан класс Ball. Затем создан объект ball: ball = Ball(canvas, 'red'). Чему равен ball.x?

2

18. Каким будет результат выполнения следующего кода re.sub(‘a’,’u’,’aeiou!’) ?

'ueiou!'