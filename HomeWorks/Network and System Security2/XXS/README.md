# Лабораторные работы по теме «Cross-Site Scripting (XSS)»

**Дисциплина:** Network and System Security  
**Направление:** 10.03.01 «Информационная безопасность»  
**Образовательная программа:** «Информационная безопасность»  
**ВУЗ:** НИУ ВШЭ, МИЭМ им. А.Н. Тихонова  

**Студент:** Новиков В. С.  
**Группа:** МКБ-241  
**Год:** 2025  

---

## 📁 Структура репозитория

```
XSS/
├── Lab01/   # Reflected XSS + WAF bypass (4.5)
├── Lab02/   # DOM-based XSS (document.write, innerHTML)
├── Lab03/   # Reflected XSS (attribute injection)
├── Lab04/   # DOM-based XSS (JavaScript context)
└── README.md
```

---

## 🧪 Lab01 — Reflected XSS в HTML-контексте при блокировке большинства тегов и атрибутов (4,5 балла)

**Платформа:** PortSwigger Web Security Academy  
**Уровень:** Practitioner  

Лабораторная работа моделирует сценарий, при котором reflected XSS уязвимость частично защищена WAF.  
Большинство HTML-тегов и JavaScript-атрибутов блокируется, что требует применения техник обхода фильтрации.

### Основные этапы
- Анализ отражения пользовательского ввода
- Фаззинг HTML-тегов и атрибутов (Burp Intruder)
- Выявление разрешённых элементов (`body`, `onresize`)
- Построение автоматического drive-by XSS через Exploit Server

### Результат
Автоматический вызов функции `print()` в браузере жертвы без взаимодействия пользователя.

---

## 🧪 Lab02 — DOM-based XSS (2 задания)

### Задание 1 — document.write (2,5 балла)

Уязвимость возникает из-за небезопасного использования `location.search` в функции `document.write`.

**Payload:**
```
"><svg onload=alert('test')>
```

### Задание 2 — innerHTML (2,5 балла)

Пользовательский ввод напрямую присваивается `innerHTML`, что приводит к выполнению HTML/JS-кода.

**Payload:**
```
<img src=1 onerror=alert('1_DOOM_XXS')>
```

---

## 🧪 Lab03 — Reflected XSS с инъекцией в атрибут при экранировании угловых скобок (2,5 балла)

Сервер экранирует угловые скобки, однако пользовательский ввод отражается внутри значения HTML-атрибута.

**Payload:**
```
" onmouseover="alert('Reflected_XXS')
```

Атака срабатывает при наведении курсора на элемент страницы.

---

## 🧪 Lab04 — DOM-based XSS в JavaScript-контексте (2,5 балла)

DOM XSS проявляется исключительно на стороне клиента из-за небезопасной обработки данных `location.search`
в JavaScript-коде страницы.

Результат — выполнение произвольного JavaScript-кода (`alert()`).

---

## 🧠 Итоги

В ходе лабораторных работ:
- изучены основные типы XSS-уязвимостей (Reflected, DOM-based);
- проанализировано влияние контекста внедрения;
- получены практические навыки работы с Burp Suite и PortSwigger Web Security Academy;
- освоены базовые методы эксплуатации XSS в реальных условиях фильтрации.

Работы демонстрируют практический подход к анализу уязвимостей веб-приложений и соответствуют учебной программе по информационной безопасности.
